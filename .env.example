# Supabase (optional - for cloud sync)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here

# Jina AI (Embeddings)
JINA_API_KEY=jina_example_key_do_not_use

# Groq (LLM Inference - recommended)
GROQ_API_KEY=gsk_example_key_do_not_use

# Deepgram (Speech-to-Text)
DEEPGRAM_API_KEY=example_key_do_not_use

# Optional: Alternative LLM providers
CLAUDE_API_KEY=example_key_do_not_use

# Optional: Alternative embedding provider
GEMINI_API_KEY=example_key_do_not_use

# Optional: Alternative TTS provider
GOOGLE_TTS_API_KEY=example_key_do_not_use

# Instructions for local development:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace example values with your real API keys
# 3. NEVER commit .env to version control (it's in .gitignore)
