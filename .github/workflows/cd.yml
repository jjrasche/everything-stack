name: Build & Test & Deploy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  # ============ Analysis ============
  # TEMPORARILY DISABLED FOR DEBUGGING - Only running emulator tests
  # analyze:
  #   name: Analyze
  #   runs-on: ubuntu-latest
  #   continue-on-error: true

  # ============ Unit Tests ============
  # TEMPORARILY DISABLED FOR DEBUGGING - Only running emulator tests
  # test:
  #   name: Unit Tests
  #   runs-on: ubuntu-latest
  #   continue-on-error: true

  # ============ Android Emulator Tests ============
  test-android-emulator:
    name: Android Emulator Tests
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Setup Android emulator
        uses: ReactiveCircus/android-emulator-runner@v2
        with:
          api-level: 29
          arch: x86_64
          profile: Nexus 5X
          script: |
            flutter pub get
            dart run build_runner build --delete-conflicting-outputs
            echo "# CI/CD test - secrets via dart-define" > .env

            # Run all integration tests
            echo "Running integration test suite..."
            flutter test integration_test/ -d emulator-5554 \
              --dart-define=GROQ_API_KEY=${{ secrets.GROQ_API_KEY }} \
              --dart-define=DEEPGRAM_API_KEY=${{ secrets.DEEPGRAM_API_KEY }} \
              --dart-define=JINA_API_KEY=${{ secrets.JINA_API_KEY }} \
              --dart-define=SUPABASE_URL=${{ secrets.SUPABASE_URL }} \
              --dart-define=SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}

  # ============ Build Web ============
  # TEMPORARILY DISABLED FOR DEBUGGING - Only running emulator tests

  # ============ Build Android APK ============
  # TEMPORARILY DISABLED FOR DEBUGGING - Only running emulator tests

  # ============ Check Crashes ============
  # TEMPORARILY DISABLED FOR DEBUGGING - Only running emulator tests

